# Pages check_site.py should not report as orphans, with the reason.
# One repo-relative path per line.

call.html    # opened from Messenger with a room id built in JS, never linked statically
kart.html    # shared by UUID link from KartTracker; deliberately not discoverable
jeopardy.html  # Ian asked for it deliberately NOT linked from the homepage or the tiles
beam.html    # experimental QR file transfer; noindex, kept off the tiles until Ian tries it on two phones
simlog.html  # private flight-sim logbook; noindex, own-only, reached by URL (not a homepage tile)
geometrydash.html  # standalone canvas arcade game; noindex, reached by URL (not yet on the PixelBreak hub)
ppt.html  # private owner-only presentation studio; noindex, reached directly by URL
doc.html  # private owner-only document studio; noindex, cross-linked only from the PPT studio
ops.html  # private owner-only Mac-mini control room; noindex, reached directly by URL
haus/index.html  # private allow-listed Haus-Planer; gated, noindex, reached from the hub tile
pro/  # private noindex client previews reached only by URL
